# Runtime (backend service) — kept lean so the production image/Render
# build only installs what api.py actually needs at request time.
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
google-genai>=1.0.0
